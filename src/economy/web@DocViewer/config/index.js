import configDocument from './config.fn.document';
import configEditor from './config.fn.editorConfig';
import configure from './config.fn';
export default {
    configEditor,
    configDocument,
    configure,
}