import React from 'react';

/**
 * 菜单权限子组件（占位）
 */
const WebMenu = ({reference}) => {
    return (
        <div style={{padding: 24}}>
            <span>菜单权限内容区（待开发）</span>
        </div>
    );
};
export default WebMenu;
