import React from 'react';

/**
 * 资源权限子组件（占位）
 */
const WebResource = ({reference}) => {
    return (
        <div style={{padding: 24}}>
            <span>资源权限内容区（待开发）</span>
        </div>
    );
};
export default WebResource;
