import React from 'react';

/**
 * 流程权限子组件（占位）
 */
const WebWorkflow = ({reference}) => {
    return (
        <div style={{padding: 24}}>
            <span>流程权限内容区（待开发）</span>
        </div>
    );
};
export default WebWorkflow;
